Этот проект — одностраничный конвертер Markdown в формат для Телеграм-чата, заточенный под личный сценарий: быстро брать ответы из ChatGPT и получать текст, который красиво и предсказуемо отображается в Telegram UI. Мы сознательно не используем MarkdownV2, а опираемся на недокументированные, но реально работающие варианты форматирования в клиенте Телеграм, чтобы избежать сложного экранирования и ломкого поведения.

Почему не MarkdownV2
MarkdownV2 требует агрессивного экранирования спецсимволов и часто ведёт себя нестабильно в реальных сообщениях.

Для постов и заметок важнее визуальный результат в UI, а не строгое соответствие Bot API.
Поэтому конвертер переводит обычный Markdown ChatGPT в «телеграмный» текст с простыми, наглядными маркерами, которые гарантированно читаются и не конфликтуют с разметкой бота.

ПРАВИЛА КОНВЕРТАЦИИ MARKDOWN → TELEGRAM UI

1. ЗАГОЛОВКИ:
   # Заголовок 1        → ▶️ Заголовок 1
   ## Заголовок 2       → 🔹 Заголовок 2
   ### Заголовок 3      → ◾ Заголовок 3
   #### Заголовок 4     → ◦ Заголовок 4

2. ФОРМАТИРОВАНИЕ ТЕКСТА:
   **текст**            → **текст** (жирный)
   *текст*              → **текст** (жирный - конвертируем в жирный)
   ***текст***          → **текст** (жирный)
   __текст__            → __текст__ (подчеркнутый)
   ~~текст~~            → ~~текст~~ (зачеркнутый)

3. СПИСКИ:
   * элемент            → - элемент (дефис вместо звездочки)
   - элемент            → - элемент (оставить как есть)

4. ЦИТАТЫ:
   > текст цитаты       → __текст цитаты__ (подчеркнутое выделение)
   После цитаты         → вставить одну пустую строку

5. КОД:
   `inline код`         → `inline код` (без изменений)
   ```блок кода```      → ```блок кода``` (без изменений)

6. ССЫЛКИ:
   [текст](url)         → [текст](url) (оставить обычными ссылками)

7. ГОРИЗОНТАЛЬНЫЕ ЛИНИИ:
   ---                  → --- (без изменений)

8. ПУСТЫЕ СТРОКИ:
   Все пустые строки    → удалять полностью,
                          КРОМЕ специально добавляемой пустой строки
                          после цитаты

9. СПЕЦСИМВОЛЫ:
   Спецсимволы          → не экранировать, использовать как есть в UI
